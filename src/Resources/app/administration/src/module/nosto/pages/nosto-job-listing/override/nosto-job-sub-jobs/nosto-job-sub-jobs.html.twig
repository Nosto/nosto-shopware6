{% block job_list_grid_job_status_link %}
    <span class="nosto-job-status-inline" :class="`is-${getStatusTone(item.status)}`">
        <span class="nosto-job-status-dot"></span>
        <span>{{ getStatusLabel(item.status) }}</span>
    </span>
{% endblock %}
