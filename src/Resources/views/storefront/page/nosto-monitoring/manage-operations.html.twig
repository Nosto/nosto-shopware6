{% extends '@Storefront/storefront/base.html.twig' %}

{% block base_body %}
    <style>
        .nosto-configuration-section {
            background: white;
            border: 1px solid #dee2e6;
            border-radius: 8px;
            margin-bottom: 20px;
        }
        .nosto-configuration-sub-section {
            border-bottom: 1px solid #f0f0f0;
            padding: 20px;
        }
        .nosto-configuration-sub-section h3 {
            margin: 0 0 20px 0;
            color: #6c757d;
            font-size: 18px;
        }
        .nosto-configuration-debug-section {
            background: white;
            border: 1px solid #dee2e6;
            border-radius: 8px;
            margin-top: 20px;
            padding: 20px;
        }
        .nosto-configuration-debug-section h3 {
            margin: 0 0 20px 0;
            color: #6c757d;
            font-size: 18px;
        }
        table {
            font-family: arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }

        td, th {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #dddddd;
        }
    </style>

    <div class="nosto-layout" style="display: flex; flex-direction: column;">
        <!-- SECTION - NOSTO VERSION -->
        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px;">
            <h2 style="margin: 0; color: #6c757d;">Nosto Monitoring (v: {{ nostoVersion }})</h2>
            <form action="{{ path('nosto-monitoring.logout') }}" method="POST" style="margin: 0;">
                <button type="submit" class="nosto-button" style="background: #dc3545; padding: 8px 16px;">
                    Logout
                </button>
            </form>
        </div>

        <!-- SECTION - CONTEXT & SALES CHANNEL SELECTION -->
        <div class="nosto-context-selectors"
             style="background: white; border: 1px solid #dee2e6; border-radius: 8px; padding: 20px; margin-bottom: 20px;">
            <h3 style="margin: 0 0 15px 0; color: #6c757d; font-size: 18px;">Context Selection</h3>
            <form method="GET" action="{{ path('nosto-monitoring.manage-operations') }}">
                <div style="display: flex; gap: 20px; flex-wrap: wrap;">
                    <!-- Language Dropdown -->
                    <div style="flex: 1; min-width: 200px;">
                        <label for="languageId"
                               style="display: block; margin-bottom: 8px; color: #8b5cf6; font-weight: 500;">Language</label>
                        <select name="languageId"
                                style="width: 100%; padding: 10px 15px; border: 1px solid #dee2e6; border-radius: 6px; background: white; font-size: 14px; color: #495057;">
                            <option value="">All Languages</option>
                            {% for language in languages %}
                                <option value="{{ language.id }}"
                                        {% if language.id == currentLanguageId %}selected{% endif %}>
                                    {{ language.locale.name }} ({{ language.locale.code }})
                                </option>
                            {% endfor %}
                        </select>
                    </div>

                    <!-- Sales Channel Dropdown -->
                    <div style="flex: 1; min-width: 200px;">
                        <label for="salesChannelId"
                               style="display: block; margin-bottom: 8px; color: #8b5cf6; font-weight: 500;">Sales
                            Channel</label>
                        <select name="salesChannelId"
                                style="width: 100%; padding: 10px 15px; border: 1px solid #dee2e6; border-radius: 6px; background: white; font-size: 14px; color: #495057;">
                            <option value="">All Sales Channels</option>
                            {% for salesChannel in salesChannels %}
                                <option value="{{ salesChannel.id }}"
                                        {% if salesChannel.id == currentSalesChannelId %}selected{% endif %}>
                                    {{ salesChannel.name }}
                                </option>
                            {% endfor %}
                        </select>
                    </div>

                    <!-- Submit Button -->
                    <div style="display: flex; align-items: end;">
                        <button type="submit" class="nosto-button"
                                style="padding: 10px 20px; background: #8b5cf6; color: white; border: none; border-radius: 6px; cursor: pointer; font-weight: 500;">
                            Apply Context
                        </button>
                    </div>
                </div>
            </form>

            <!-- Current Context Display -->
            {% if currentLanguageId or currentSalesChannelId %}
                <div style="margin-top: 15px; padding-top: 15px; border-top: 1px solid #f0f0f0;">
                    <div style="display: flex; gap: 20px; font-size: 14px; color: #6c757d; flex-wrap: wrap;">
                        {% if currentLanguageId %}
                            <span><strong>Active Language:</strong>
                                    {% for language in languages %}
                                        {% if language.id == currentLanguageId %}
                                            {{ language.locale.name }} ({{ language.locale.code }})
                                        {% endif %}
                                    {% endfor %}
                                </span>
                        {% endif %}
                        {% if currentSalesChannelId %}
                            <span><strong>Active Sales Channel:</strong>
                                    {% for salesChannel in salesChannels %}
                                        {% if salesChannel.id == currentSalesChannelId %}
                                            {{ salesChannel.name }}
                                        {% endif %}
                                    {% endfor %}
                                </span>
                        {% endif %}
                    </div>
                </div>
            {% endif %}
        </div>
        <div class="nosto-main">
            <div class="flash-messages">
                {% for type, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="nosto-message nosto-{{ type }}">
                            {{ message }}
                        </div>
                    {% endfor %}
                {% endfor %}
            </div>
            <div id="nosto-success-message" class="nosto-message" style="display: none;">
                Success message! Green color modern
            </div>
        </div>

        <!-- SECTION - CLEAR JOBS -->
        <div class="nosto-clear-form-wrapper" style="margin-bottom: 20px;">
            <form id="nosto-clear-form" action="{{ path('nosto-monitoring.clear-jobs') }}" method="POST">
                <button type="submit" class="nosto-button">Clear Nosto Jobs</button>
            </form>
        </div>

        <!-- SECTION - CONFIGURATION -->
        <div class="nosto-configuration-section">
            <!-- SECTION - ACCOUNT SETTINGS -->
            <div class="nosto-configuration-sub-section nosto-account-settings">
                <h3>Account settings</h3>
                {{ dump(nostoConfig.accountSettings) }}
            </div>

            <!-- SECTION - GENERAL SETTINGS -->
            <div class="nosto-configuration-sub-section">
                <h3>General Settings</h3>
                {{ dump(nostoConfig.generalSettings) }}
            </div>

            <!-- SECTION - TAGS ASSIGNMENT -->
            <div class="nosto-configuration-sub-section">
                <h3>Tags Assignment</h3>
                {{ dump(nostoConfig.tagsAssignment) }}
            </div>

            <!-- SECTION - FEATURE FLAGS -->
            <div class="nosto-configuration-sub-section">
                <h3>Features flags</h3>
                {{ dump(nostoConfig.featureFlags) }}
            </div>
        </div>

        <!-- SECTION - LOGS -->
        <div class="nosto-configuration-section-logs">
            <form id="nosto-logs" action="{{ path('nosto-monitoring.logs') }}" method="GET">
                <button type="submit" class="nosto-button">Logs</button>
            </form>
            <form id="nosto-download-logs">
                <button onclick="handleLogsDownload()" class="nosto-button">Download log files</button>
            </form>
        </div>

        <!-- SECTION - DEBUG INSTANCES -->
        <div class="nosto-configuration-debug-section">
            <h3>Debug Resources</h3>

            <div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 20px;">
                <!-- Product Debug Form -->
                <div>
                    <form action="{{ path('nosto-monitoring.show-product') }}" method="POST">
                        <div style="margin-bottom: 15px;">
                            <label style="color: #007bff; font-weight: 500; display: block; margin-bottom: 8px;">Product ID</label>
                            <input
                                    type="text"
                                    name="productId"
                                    style="width: 100%; padding: 8px 12px; border: 1px solid #e9ecef; border-radius: 4px; background: #fff;"
                                    placeholder="Enter product ID"
                                    required
                            >
                        </div>
                        <button type="submit" class="nosto-button" style="width: 100%;">Show Product</button>
                    </form>
                </div>

                <!-- Category Debug Form -->
                <div>
                    <form action="{{ path('nosto-monitoring.show-category') }}" method="POST">
                        <div style="margin-bottom: 15px;">
                            <label style="color: #007bff; font-weight: 500; display: block; margin-bottom: 8px;">Category ID</label>
                            <input type="hidden" name="languageId" value="{{ app.request.get('languageId') }}">
                            <input type="hidden" name="salesChannelId" value="{{ app.request.get('salesChannelId') }}">
                            <input
                                    type="text"
                                    name="categoryId"
                                    style="width: 100%; padding: 8px 12px; border: 1px solid #e9ecef; border-radius: 4px; background: #fff;"
                                    placeholder="Enter category ID"
                                    required
                            >
                        </div>
                        <button type="submit" class="nosto-button" style="width: 100%;">Show Category</button>
                    </form>
                </div>

                <!-- Order Debug Form -->
                <div>
                    <form action="{{ path('nosto-monitoring.show-order') }}" method="POST">
                        <div style="margin-bottom: 15px;">
                            <label style="color: #007bff; font-weight: 500; display: block; margin-bottom: 8px;">Order ID</label>
                            <input
                                    type="text"
                                    name="orderId"
                                    style="width: 100%; padding: 8px 12px; border: 1px solid #e9ecef; border-radius: 4px; background: #fff;"
                                    placeholder="Enter order ID"
                                    required
                            >
                        </div>
                        <button type="submit" class="nosto-button" style="width: 100%;">Show Order</button>
                    </form>
                </div>
            </div>
        </div>

    </div>

    <script>
        window.router['nosto-monitoring.log-download-all'] = '{{ path('nosto-monitoring.log-download-all') }}';
        async function detectIncognito() {
            return new Promise(function (resolve, reject) {
                let browserName = 'Unknown';

                function __callback(isPrivate) {
                    resolve({
                        isPrivate,
                        browserName
                    });
                }

                function identifyChromium() {
                    const ua = navigator.userAgent;
                    if (ua.match(/Chrome/)) {
                        if (navigator.brave !== undefined) {
                            return 'Brave';
                        } else if (ua.match(/Edg/)) {
                            return 'Edge';
                        } else if (ua.match(/OPR/)) {
                            return 'Opera';
                        }
                        return 'Chrome';
                    } else {
                        return 'Chromium';
                    }
                }

                function feid() {
                    let toFixedEngineID = 0;
                    try {
                        eval('(-1).toFixed(-1);');
                    } catch (e) {
                        toFixedEngineID = e.message.length; // Safari 44, Chrome 51, Firefox 25
                    }
                    return toFixedEngineID;
                }

                function isSafari() {
                    return feid() === 44;
                }

                function isChrome() {
                    return feid() === 51;
                }

                function isFirefox() {
                    return feid() === 25;
                }

                function isMSIE() {
                    return (
                        navigator.msSaveBlob !== undefined &&
                        eval.toString().length === 39
                    );
                }

                function newSafariTest() {
                    const tmp_name = String(Math.random());

                    try {
                        const db = window.indexedDB.open(tmp_name, 1);

                        db.onupgradeneeded = function (i) {
                            const res = i.target.result;

                            try {
                                res.createObjectStore('test', {autoIncrement: true}).put(new Blob());
                                __callback(false);
                            } catch (e) {
                                const message = e.message || e;
                                const matchesExpectedError = message.includes('BlobURLs are not yet supported');
                                __callback(matchesExpectedError);
                            } finally {
                                res.close();
                                window.indexedDB.deleteDatabase(tmp_name);
                            }
                        };
                    } catch (e) {
                        __callback(false);
                    }
                }

                function oldSafariTest() {
                    const openDB = window.openDatabase;
                    const storage = window.localStorage;

                    try {
                        openDB(null, null, null, null);
                    } catch (e) {
                        __callback(true);
                        return;
                    }

                    try {
                        storage.setItem('test', '1');
                        storage.removeItem('test');
                    } catch (e) {
                        __callback(true);
                        return;
                    }

                    __callback(false);
                }

                function safariPrivateTest() {
                    if (navigator.maxTouchPoints !== undefined) {
                        newSafariTest();
                    } else {
                        oldSafariTest();
                    }
                }

                function getQuotaLimit() {
                    if (
                        performance.memory &&
                        performance.memory.jsHeapSizeLimit
                    ) {
                        return performance.memory.jsHeapSizeLimit;
                    }
                    return 1073741824;
                }

                function storageQuotaChromePrivateTest() {
                    navigator.webkitTemporaryStorage.queryUsageAndQuota(
                        function (_, quota) {
                            const quotaInMib = Math.round(quota / (1024 * 1024));
                            const quotaLimitInMib = Math.round(getQuotaLimit() / (1024 * 1024)) * 2;

                            __callback(quotaInMib < quotaLimitInMib);
                        },
                        function (e) {
                            reject(
                                new Error(
                                    'detectIncognito failed to query storage quota: ' +
                                    e.message
                                )
                            );
                        }
                    );
                }

                function oldChromePrivateTest() {
                    const fs = window.webkitRequestFileSystem;
                    fs(0, 1, function () {
                        __callback(false);
                    }, function () {
                        __callback(true);
                    });
                }

                function chromePrivateTest() {
                    if (self.Promise && self.Promise.allSettled) {
                        storageQuotaChromePrivateTest();
                    } else {
                        oldChromePrivateTest();
                    }
                }

                function firefoxPrivateTest() {
                    __callback(navigator.serviceWorker === undefined);
                }

                function msiePrivateTest() {
                    __callback(window.indexedDB === undefined);
                }

                function main() {
                    if (isSafari()) {
                        browserName = 'Safari';
                        safariPrivateTest();
                    } else if (isChrome()) {
                        browserName = identifyChromium();
                        chromePrivateTest();
                    } else if (isFirefox()) {
                        browserName = 'Firefox';
                        firefoxPrivateTest();
                    } else if (isMSIE()) {
                        browserName = 'Internet Explorer';
                        msiePrivateTest();
                    } else {
                        reject(new Error('detectIncognito cannot determine the browser'));
                    }
                }

                main();
            });
        }

        if (typeof window !== 'undefined') {
            window.detectIncognito = detectIncognito;
        }

        async function handleLogsDownload() {
            const result = await window.detectIncognito();

            if (result.isPrivate) {
                alert('Downloading log files is not allowed in incognito mode.');
                return;
            }

            const apiRoute = window.router['nosto-monitoring.log-download-all'];

            const downloadUrl = window.location.origin + apiRoute;

            const a = document.createElement('a');
            a.href = downloadUrl;
            document.body.appendChild(a);
            a.click();
            document.body.removeChild(a);
        }
    </script>
{% endblock %}
