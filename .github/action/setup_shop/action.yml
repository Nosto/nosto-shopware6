name: 'Setup shop'
description: 'Install the Shopware shop with plugin'
inputs:
  shopware-version:
    description: 'Version for the Shopware installation'
    required: true

runs:
  using: 'composite'
  steps:
    - name: Create shop
      shell: bash
      run: shopware-cli project create shopware ${{ inputs.shopware-version }}

    - name: Fix Twig compatibility before installation
      shell: bash
      run: |
        cd shopware
        echo "=== Before fix ==="
        composer show twig/twig squirrelphp/twig-php-syntax

        # Downgrade to compatible versions
        composer require --no-update twig/twig:^3.4.0
        composer require --no-update squirrelphp/twig-php-syntax:^1.8
        composer update --no-progress --no-interaction twig/twig squirrelphp/twig-php-syntax

        echo "=== After fix ==="
        composer show twig/twig squirrelphp/twig-php-syntax

    - name: Install Shopware
      shell: bash
      run: |
        cp nosto-shopware6/.github/.env.local shopware/.env.local
        cp nosto-shopware6/.github/.env.local shopware/.env.test
        cd shopware
        ./bin/console system:install --basic-setup

    - name: Install plugin
      shell: bash
      run: |
        cp -r nosto-shopware6 shopware/custom/plugins
        cd shopware
        php bin/console plugin:refresh
        php bin/console plugin:install --activate --clearCache NostoIntegration
