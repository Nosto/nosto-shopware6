name: 'Setup shop'
description: 'Install the Shopware shop with plugin'
inputs:
  shopware-version:
    description: 'Version for the Shopware installation'
    required: true

runs:
  using: 'composite'
  steps:
    - name: Create shop
      shell: bash
      run: shopware-cli project create shopware ${{ inputs.shopware-version }}

    - name: Fix Twig compatibility before installation
      shell: bash
      run: |
        cd shopware
        echo "=== Current versions ==="
        composer show twig/twig
        composer show squirrelphp/twig-php-syntax
        
        echo "=== Applying targeted fix for method visibility and return type ==="
        
        # Create a proper backup first
        cp vendor/squirrelphp/twig-php-syntax/src/TokenParser/ForeachTokenParser.php vendor/squirrelphp/twig-php-syntax/src/TokenParser/ForeachTokenParser.php.backup
        
        # Show the current problematic line
        echo "=== Current line 68 ==="
        sed -n '68p' vendor/squirrelphp/twig-php-syntax/src/TokenParser/ForeachTokenParser.php
        
        # Fix 1: Change private to protected on line 68
        sed -i '68s/private function parseAssignmentExpression/protected function parseAssignmentExpression/' vendor/squirrelphp/twig-php-syntax/src/TokenParser/ForeachTokenParser.php
        
        # Fix 2: Change return type from Node to Nodes on line 68
        sed -i '68s/: Node/: Nodes/' vendor/squirrelphp/twig-php-syntax/src/TokenParser/ForeachTokenParser.php
        
        # Verify the changes
        echo "=== After fixes - line 68 ==="
        sed -n '68p' vendor/squirrelphp/twig-php-syntax/src/TokenParser/ForeachTokenParser.php
        
        # Now we need to fix the method to actually return Nodes instead of Node
        # Looking at the method, it builds an array and then returns new Node at the end
        # We need to find where it returns Node and wrap it in Nodes
        
        echo "=== Looking for the return statement ==="
        grep -n -A 3 -B 3 "return new Node" vendor/squirrelphp/twig-php-syntax/src/TokenParser/ForeachTokenParser.php
        
        # Find the end of the method to see what it returns
        echo "=== End of parseAssignmentExpression method ==="
        sed -n '85,93p' vendor/squirrelphp/twig-php-syntax/src/TokenParser/ForeachTokenParser.php
        
        # Check PHP syntax
        echo "=== Checking PHP syntax ==="
        php -l vendor/squirrelphp/twig-php-syntax/src/TokenParser/ForeachTokenParser.php
        
        echo "=== Partial fix applied - may need return statement adjustment ==="

    - name: Install Shopware
      shell: bash
      run: |
        cp nosto-shopware6/.github/.env.local shopware/.env.local
        cp nosto-shopware6/.github/.env.local shopware/.env.test
        cd shopware
        ./bin/console system:install --basic-setup

    - name: Install plugin
      shell: bash
      run: |
        cp -r nosto-shopware6 shopware/custom/plugins
        cd shopware
        php bin/console plugin:refresh
        php bin/console plugin:install --activate --clearCache NostoIntegration
